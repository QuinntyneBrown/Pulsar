{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Comment": "Loopback only: Pulsar loads and runs user-supplied assemblies, so it must not be exposed to the network. Change this only if you have added authentication.",
        "Url": "http://127.0.0.1:5179"
      }
    }
  },
  "Pulsar": {
    "PluginPath": "plugins/manifest/pulsar.plugin.json",
    "RedisConnectionString": "localhost:6379"
  }
}
